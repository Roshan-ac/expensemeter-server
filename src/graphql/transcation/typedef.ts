export const typeDefs = `#graphql
   

type Transcation {
        id: ID
        moneyType: String
        roles: String
        authorId:String
        amount: Int
        friend: String
        remarks: String
    }
`;
